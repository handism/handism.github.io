# Workflows

このディレクトリには、プロジェクトで共通して利用されるワークフローの定義を格納します。

## ワークフローの追加方法

必要に応じて、特定の手順（例：ビルドからデプロイ、テスト実行、PR作成など）を自動化・標準化するためのワークフローファイルをここに配置できます。
例として `release.md` や `test-pipeline.md` などの Markdown ファイルを作成し、エージェントへの明確な手順指示を記述します。
